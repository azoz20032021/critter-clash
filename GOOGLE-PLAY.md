# 🚀 خطوات رفع اللعبة على Google Play

دليل مرتّب بالترتيب الفعلي للتنفيذ. الأشياء التي **لا يمكن لأحد أن يعملها بدلاً عنك** معلّمة بـ 🔴.

> **الخبر الجيد:** لا تحتاج Android Studio ولا Java ولا Android SDK على جهازك.
> ملف `.github/workflows/android.yml` يبني اللعبة داخل سحابة GitHub مجاناً.

---

## المرحلة ١ — ارفع الكود على GitHub

```bash
cd critter-clash
git push -u origin main
```

المستودع جاهز محلياً بـ commit كامل، والـ remote مضبوط. إن لم يفتح المتصفح تلقائياً،
أنشئ المستودع يدوياً من https://github.com/new باسم `critter-clash` (**خاص**، بدون README).

---

## المرحلة ٢ — احصل على APK وجرّبها على جوالك (١٠ دقائق، مجاناً)

هذه أهم خطوة قبل أي شيء: **العب اللعبة على جهاز حقيقي**.

1. في المستودع → تبويب **Actions**
2. من اليسار اختر **build android** → زر **Run workflow** → **Run workflow**
3. انتظر ٥–١٠ دقائق (البناء الأول أبطأ)
4. افتح التشغيل المكتمل → قسم **Artifacts** → نزّل **`critter-clash-debug-apk`**
5. فك الضغط، وانقل `app-debug.apk` إلى جوالك وثبّتها
   (فعّل «السماح بتثبيت من مصادر غير معروفة» عند الطلب)

جرّب: النقر، الزعماء، البعث، الطفرات، الحلبة، الحفظ بعد إغلاق التطبيق، والاهتزاز.

---

## المرحلة ٣ — 🔴 غيّر الأشياء التي لا رجعة فيها

**قبل** أي رفع للمتجر:

| الملف | ماذا تغيّر | لماذا |
|---|---|---|
| `capacitor.config.json` | `appId` → `com.اسمك.critterclash` | **لا يمكن تغييره أبداً بعد النشر** |
| `capacitor.config.json` | `appName` | الاسم الظاهر تحت الأيقونة |
| `www/js/ads.js` | معرّفات AdMob + `USE_TEST_ADS = false` | حالياً معرّفات اختبار من Google لا تدفع شيئاً |
| `www/js/main.js` | احذف كتلة `CCDEBUG` (اختياري) | أدوات مطوّر مكشوفة |

بعدها: `git add -A && git commit -m "release config" && git push`

---

## المرحلة ٤ — 🔴 مفتاح التوقيع (مرة واحدة للأبد)

على أي جهاز فيه Java (أو من داخل Android Studio / Git Bash على ويندوز):

```bash
keytool -genkeypair -v -keystore critterclash.jks -alias critterclash \
        -keyalg RSA -keysize 2048 -validity 10000
```

سيسألك عن كلمة مرور واسمك — احفظها في مدير كلمات مرور.

> ⚠️ **احتفظ بملف `critterclash.jks` للأبد وخذ منه نسخة احتياطية.**
> فقدانه = لا تستطيع تحديث لعبتك على المتجر مدى الحياة. هذا أشهر خطأ يقع فيه المطورون الجدد.

حوّله إلى نص:

```bash
base64 -w0 critterclash.jks > critterclash.b64     # ماك: base64 -i critterclash.jks
```

ثم في GitHub: **Settings → Secrets and variables → Actions → New repository secret**،
وأضف أربعة أسرار:

| الاسم | القيمة |
|---|---|
| `KEYSTORE_BASE64` | محتوى ملف `critterclash.b64` |
| `KEYSTORE_PASSWORD` | كلمة مرور المخزن |
| `KEY_ALIAS` | `critterclash` |
| `KEY_PASSWORD` | كلمة مرور المفتاح |

شغّل **build android** مرة ثانية → الآن ستحصل على **`critter-clash-release-aab`**،
وهذا هو الملف الذي يرفع للمتجر.

---

## المرحلة ٥ — 🔴 حساب Google Play Console

1. https://play.google.com/console → **٢٥$ مرة واحدة مدى الحياة**
2. اختر نوع الحساب:
   - **Personal** (فردي): يتطلب **١٢ مختبِراً لمدة ١٤ يوماً متواصلاً** قبل السماح بالنشر العام
   - **Organization** (منظمة): بلا شرط المختبرين، لكن يحتاج رقم D-U-N-S ويأخذ وقتاً أطول
3. أكمل التحقق من الهوية (بطاقة + عنوان) — قد يستغرق ١–٣ أيام

> ابدأ هذه المرحلة **اليوم** حتى لو اللعبة غير جاهزة، لأن التحقق يأخذ وقتاً.

---

## المرحلة ٦ — أنشئ التطبيق وارفع

في Play Console → **Create app**:

- اسم التطبيق: `صدام المخلوقات` أو `Critter Clash`
- اللغة الافتراضية، النوع: **Game**، السعر: **Free**

ثم املأ (كلها إجبارية قبل النشر):

| القسم | ما تحتاجه | من أين |
|---|---|---|
| **Store listing** | وصف قصير وطويل بالعربي والإنجليزي | جاهز في `PUBLISHING.md` |
| **Graphics** | أيقونة ٥١٢×٥١٢ + Feature graphic ١٠٢٤×٥٠٠ + لقطتان على الأقل | جاهزة في `resources/` و `shots/` |
| **Privacy policy** | رابط عام | ارفع `docs/privacy.html` على مستودع عام صغير |
| **Content rating** | استبيان قصير | أجب: لا عنف واقعي، لا محتوى جنسي → **Everyone** |
| **Target audience** | الفئة العمرية | ١٣+ (لأن فيه إعلانات) |
| **Data safety** | استبيان البيانات | الجدول في `PUBLISHING.md` §٥ |
| **Ads** | «هل يحتوي التطبيق على إعلانات؟» | ✅ **نعم** — لا تنسها |
| **App access** | هل يحتاج تسجيل دخول؟ | لا، كل المحتوى مفتوح |

ثم: **Testing → Internal testing → Create release** → ارفع ملف `.aab`.

جرّبها من الرابط الداخلي على جوالك، وبعد ما تتأكد أن كل شي تمام:
**Production → Create release → Rollout**.

---

## المرحلة ٧ — بعد النشر

- المراجعة الأولى تأخذ عادة **١–٧ أيام**
- كل تحديث لاحق: ارفع `version` في `package.json`، شغّل الـ workflow، ارفع الـ `.aab` الجديد
- اربط AdMob بحساب Play Console (يحسّن دقة الإعلانات)
- راقب **Android vitals** — تعطلات فوق ١٫٠٩٪ تخفض ترتيبك في المتجر

---

## ملخص التكلفة والوقت

| البند | التكلفة | الوقت |
|---|---|---|
| Google Play Console | ٢٥$ مرة واحدة | تحقق ١–٣ أيام |
| GitHub Actions | مجاني | ٥–١٠ دقائق لكل بناء |
| AdMob | مجاني | يوم للموافقة |
| مراجعة المتجر | مجاني | ١–٧ أيام |
| **حساب فردي:** فترة المختبرين الإجبارية | مجاني | **١٤ يوماً + ١٢ مختبراً** |

**أسرع مسار واقعي للنشر العام: من ٣ أسابيع إلى شهر** — أغلبه انتظار إجراءات Google، وليس عملاً منك.

---

## ماذا عن iOS؟

يتطلب جهاز **Mac** فعلياً (Xcode لا يعمل على غيره) + ٩٩$ سنوياً.
نصيحتي: انشر على أندرويد أولاً، وإن نجحت اللعبة استأجر Mac سحابي أو استخدم جهاز صديق.
كل الكود جاهز لـ iOS بالفعل — `npx cap add ios` وخلاص.
